const config = {
	apiUrl: `http://johncheesman.org.uk/quartz-feed/api/`,
	endpoints: {
		getFeed: `getFeed`,
		getAvailableFeeds: `getAvailableFeeds`,
		getAvailableFeedItems: `getAvailableFeedItems`
	}
};

export { config };